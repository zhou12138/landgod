{
  "id": "landgod",
  "name": "LandGod",
  "description": "Enterprise execution network — call tools, inspect workers, dispatch tasks, and audit execution through LandGod Gateway / MCPHub",
  "configSchema": {
    "type": "object",
    "properties": {
      "gatewayUrl": {
        "type": "string",
        "default": "http://localhost:8081",
        "description": "LandGod Gateway HTTP API URL"
      }
    },
    "additionalProperties": false
  }
}
